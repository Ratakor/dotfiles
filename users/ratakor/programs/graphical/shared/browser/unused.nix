# Unused & unconfigured browsers
{
  programs = {
    # check out celenityy/phoenix
    firefox = {
      enable = false;
    };

    # browser for lisp people
    nyxt = {
      enable = false;
    };

    # "minimal" vim-like browser
    qutebrowser = {
      enable = false;
    };
  };
}

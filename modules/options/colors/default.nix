# TODO: make that an actual option and ig access it through config.colors
{
  gruvbox-dark = import ./gruvbox-dark.nix;
  gruvbox-light = import ./gruvbox-light.nix;
  dracula = import ./dracula.nix;
}

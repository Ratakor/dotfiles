#!/bin/sh
[ -n "$1" ] && MUSIC="$1" || MUSIC="$HOME/music/$(ls $HOME/music | dmenu -c -p 'Play ')"
[ $(printf 'no\nyes' | dmenu -c -p 'Shuffle?') = "yes" ]  && SHUFFLE='--shuffle'
mpv --no-video --loop-playlist "$SHUFFLE" "$MUSIC" & vis

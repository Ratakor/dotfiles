#!/bin/sh
# menu for shutdown or lock
# require nopass for poweroff and reboot

QUERY=$(printf 'no\nlock\npoweroff\nreboot' | dmenu -i -sb '#ff5555' -p 'Shutdown?')
ROOTCMD="${ROOTCMD:-$(command -v doas || command -v sudo)}"

case "$QUERY" in
"lock")
	slock ;;
"poweroff")
	"$ROOTCMD" poweroff ;;
"reboot")
	"$ROOTCMD" reboot ;;
esac

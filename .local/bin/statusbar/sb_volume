#!/bin/bash
#printf "$(amixer get Master | tail -n1 | sed -r 's/.*\[(.*)%\].*/\1/')%%"
ICON_ON=''
ICON_OFF=''
VOLUME=$(pamixer --get-volume)

[ $VOLUME = 0 ] && printf "\x03$ICON_OFF MUTE\x01\n" || printf "$ICON_ON $VOLUME%%\n"

#!/bin/sh
# color username and save history for sic

HIST="${XDG_DATA_HOME:-$HOME/.local/share}/sic"
mkdir -p "$HIST"
HIST="$HIST/$(date '+sic-%Y-%m-%d-%H:%M-%S')"

sic "$@" | tee -a "$HIST" | awk "{gsub(\"$USER\",\"\033[1;34m$USER\033[m\")}1"

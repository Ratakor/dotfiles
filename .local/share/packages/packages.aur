cli-visualizer # audio visualizer
xbanish # hide mouse when typing
ungoogled-chromium-xdg-bin # ungoogled chromium but xdg compliant
#xdg-ninja # tool to check if a home directory can be xdg compliant (e.g. .ssh)
#oneko # cat that follow mouse cursor
dashbinsh # a pacman hook to symlink dash to sh when there is a bash update
#openssh-xdg # openssh but xdg compliant
#sc-im # sc with vim keybind
#cyrus-sasl-xoauth2-git # xoauth for mutt
dracula-gtk-theme
#gomuks # matrix client
